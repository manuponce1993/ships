/**
 * * Enum de las URLs de la aplicación
 */
export enum ROUTES {
   LOGIN = '/login',
   HOME = '/app/principal',
   CASES = '/app/casos',
   CONTACT = '/app/contacto',
   ATTENTIONS = '/app/atenciones',
   NEWS = '/app/novedades',
}
